extern char text[][25]; // we dont have to specify the size of the first array [] but the next we have to specify



void foo(void){
extern int i; // we can also declare a extern variabels inside functions

	i = 100;
}