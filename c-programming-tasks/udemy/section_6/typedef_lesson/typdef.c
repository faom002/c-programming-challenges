
#define int_pointer int *


int_pointer chalk, cheese;


typedef char *char_ptr;

char_ptr Bently, Rolls;

// Note! that whenever we initialize define variable the first variable will be the same as the define but after (,) it will not
// but with typedef all of the variables will be of that type typedef 

int main(int argc, char const *argv[])
{
	

// i_pointer p; // same as int* p

// i_pointer a, *b; // same as int *a,**b;

	return 0;
}